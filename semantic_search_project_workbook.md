# Проект: семантический поиск товаров по текстовому запросу (query-to-item)

> Рабочий документ заполняется только после выполнения соответствующих ячеек
> Jupyter. Здесь не должно быть результатов из памяти, старого запуска или
> неподтверждённых предположений.

## О проекте

**Бизнес-контекст.** CalmFruits проверяет, улучшает ли векторный поиск товаров
по текстовому запросу существующий поиск по ключевым словам.

**Задача MVP.** Query-to-item: по тексту пользовательского запроса вернуть
top-K карточек товара и в дальнейшем сравнить лексический и семантический
поиск на одинаковом полном каталоге.

**Письменный контракт.** Требования зафиксированы в `AGENTS.md` и
`context/project-month/`. QA-сессия в этой реализации не проводится по
явному решению пользователя.

## Неделя 1 — аудит данных и EDA

### Что выполнено

- [x] Тетрадь `notebooks/01_data_audit_eda.ipynb` выполнена сверху вниз в
  чистом kernel `calmfruits`.
- [x] Реальные outputs тетради проверены и сохранены в notebook.
- [x] Таблицы решений сохранены в `artifacts/eda/`.

### Наблюдения по каталогу

Заполнять после выполнения разделов 2–7 EDA-тетради. Для каждого пункта
указать номер раздела и имя объекта с доказательством.

| Вопрос | Фактический ответ и ссылка на доказательство |
| --- | --- |
| Размер, `imt_id`, `nm_id`, дубликаты | `catalog_structure` в разделе 2: 200 000 строк, 174 427 уникальных `imt_id`, 199 957 `nm_id`, 0 полных дубликатов; 25 573 строки повторяют `imt_id`. У 95% карточек не более двух артикулов, максимум — 30. |
| Пропуски, пустые и пробельные строки | `products_missing` и `strict_indexability` в разделе 3: пусты `description` у 57 509 строк (28,75%), `imt_name` у 708 (0,35%), цвет у 34 226 (17,11%), бренд у 201 (0,10%). Обоими обязательными полями обладают 141 946 строк (70,97%). |
| Корневые и предметные категории | `root_category_rows`, `root_category_imt`, `subj_category_imt` и график раздела 4: крупнейшие корни по строкам — «Одежда» (43 316), «Аксессуары» (16 446), «Дом» (15 024), «Обувь» (13 187); в хвосте есть предметные категории ровно с одной карточкой. |
| Длины `imt_name` и `description`, выбросы | `name_lengths`, `description_lengths`, `longest_descriptions` и график раздела 5: медианы — 4 слова/31 символ в названии и 53 слова/398 символов в описании; 99-й перцентиль описаний — 321 слово/2 353 символа, максимум — 754 слова/5 000 символов. |
| Ручная проверка 20–30 карточек и текстовые артефакты | `manual_sample`, сохранённый `manual_review_cards.html`, `text_diagnostics`, `diagnostic_examples` и `preview_rows` в разделе 6: 22 описания имеют HTML, 26 546 названий (13,27%) содержат специальные символы, 14 888 (7,44%) — кандидаты на буквенно-цифровые токены с буквами и цифрами. Полные примеры `imt_id=10027164` и `imt_id=10021271` показывают, что технические обозначения могут быть смысловыми. Preview не содержит `None`/`nan`. |
| Бренды и повторение цветов в названиях | `attribute_summary` и выборка `color_in_name` в разделе 7: 13 318 непустых брендов; цвет указан у 165 774 строк, из них у 165 130 заполнено название. Совпадение цвета с названием по консервативному лексико-морфологическому правилу — у 6 506 (3,94% строк с обоими полями). |

### Наблюдения по тренировочной разметке

Заполнять после раздела 8 EDA-тетради; тестовая разметка не используется.

| Вопрос | Фактический ответ и ссылка на доказательство |
| --- | --- |
| Пары «запрос—товар», уникальные запросы и оценённые товары на запрос | `query_inventory` и `items_per_query` в разделе 8: 11 453 пары, 700 запросов, 2 285 оценённых товаров, без повторных пар; медиана — 16 оценённых товаров на запрос. |
| Распределение `relevance` | `queries_train['relevance'].value_counts()` в разделе 8: оценка 1 — 9 652 пары, 2 — 1 101, 3 — 700; значения 0 отсутствуют в предоставленном train. |
| Покрытие `item_id → imt_id` | `query_item_coverage` в разделе 8: все 11 453 размеченные пары и все соответствующие `item_id` присутствуют в сыром каталоге. |
| Характер текстов запросов | Выборка `query_text` в разделе 8 содержит короткие товарные запросы с категорией, атрибутами и брендами; это q2i-вход, а не полные тексты карточек. |

### Принятые решения недели

| Решение | Правило | Статус/доказательство |
| --- | --- | --- |
| Уровень индексации | Одна карточка на `imt_id`. | Зафиксировано письменными требованиями; реализация дедупликации — неделя 2. |
| Вход в индекс | Обязательны непустые `imt_name` **и** `description`; это строгое правило `AGENTS.md`, имеющее приоритет над менее строгой рекомендацией урока. | Число исключённых карточек будет внесено по `strict_indexability`. |
| Корневые категории MVP | До 3 категорий по `mvp_category_ranking`: relevant train-query coverage → число пригодных `imt_id` → имя категории. | Выбраны «Одежда» (489 релевантных train-запросов, 14 270 пригодных `imt_id`) и «Обувь» (211, 3 697). Категории без релевантных train-запросов исключаются. |
| Варианты `product_text` | Сравнены `imt_name`, `imt_name + subj_name`, `imt_name + subj_name + description` на 141 946 пригодных строках. | `product_text_options` раздела 9: медианная/95-процентильная длина — 5/13, 8/16 и 63/197 слов соответственно. |
| Исходный `product_text` | `imt_name + " . " + subj_name + " . " + description`; необязательные пустые поля не добавляются. | Выбран по `product_text_options`: сохраняет тип, назначение и детали; очистка и политика обрезки — неделя 2. |
| Бренд и цвет | Не включать в baseline до доказательства пользы; сохранить как кандидаты для дальнейших экспериментов. | Сверить с разделом 7 EDA. |
| Будущая бинаризация | `relevance >= 2`; NDCG считать по исходной шкале 0–3. | Зафиксировано для недели 2. |
| Будущие значения K | `(1, 3, 5, 10)`. | Зафиксировано для недели 2. |

### Найденные проблемы и риски для недели 2

Сформировано по `text_diagnostics`, `diagnostic_examples`, `manual_sample` и
`longest_descriptions` текущего запуска:

- Перед TF-IDF и эмбеддингами очистить HTML и повторяющиеся пробелы; не
  удалять технические токены и модельные обозначения без отдельного правила:
  ручная выборка содержит смысловые `R 8.6 D -6.00`, `TORX T70`, `100мм`.
- Дедупликация по `imt_id` обязательна; при конфликте описаний выбирать
  информативную строку детерминированно.
- Описания имеют длинный хвост до 754 слов, поэтому во второй неделе явно
  сопоставить политику обрезки с лимитом выбранной модели.
- Строгое правило отбора исключит 58 054 строки; влияние этого сокращения
  каталога необходимо зафиксировать в baseline.
- Цвет не включать в baseline `product_text`: по текущей диагностике его
  консервативное совпадение с названием встречается редко, а влияние следует
  проверять отдельным экспериментом.

### Открытые вопросы

- Доступ к S3 должен быть предоставлен через переменные окружения из
  `.env.example`; секреты не входят в репозиторий.
- Доступ к S3 активирован через Практикум; `.env` хранится только локально и
  исключён из Git.
